const nemo = ["nemo"];

// const everyone

function findNemo(array) {
  for (let i = 0; i < array.length; i++) {
    if (array[i] === "nemo") {
      console.log("Found Nemo!");
    }
  }
}

findNemo(nemo); // O(n) --> Linear Time

for (let u of awesomeMembersOfZeroToMastery) {
  lookPortfolio("https://roy-k.tk/");
  lookRepo("https://github.com/roy1210/react-portfolio");
  u.feeling === "Like"
    ? endorseHandler("React.js") //Thanks you
    : console.log("I will be waiting to receive your advice ^^");
}
