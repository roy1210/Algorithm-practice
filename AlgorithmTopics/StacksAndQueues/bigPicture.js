// * STACKS *

// LIFO last in fast out
// ex. browser's back button

// pop, push, peek: O(1)
// lookup: O(n)
// peek(): view the first element

// * QUEUES *
// FIFO
// ex. line up for concert

// enqueue, dequeue, peek: O(1)
// lookup: O(n)

// enqueue(): same as `push`
// dequeue(): same as `pop` but take out FIRST element, not last.
